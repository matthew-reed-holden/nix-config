# Monitor configuration.
# Format: monitor = NAME, RESOLUTION@RATE, POSITION, SCALE
# https://wiki.hyprland.org/Configuring/Monitors/

# Primary: Acer XZ320Q X (1920x1080, supports up to 240Hz).
monitor = HDMI-A-2, 1920x1080@240, 0x0, 1

# Fallback for any unmatched output (e.g. plug in a laptop / TV).
monitor = , preferred, auto, auto
